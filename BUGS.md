# Open issues / tech debt

Local development: see [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) — the
Docker stack repros wizard-flow bugs (BUG-15, BUG-22) without deploy
round-trips.

A lightweight log of cross-cutting issues that don't fit a single PR.
For per-bug detail see commit history (search `BUG-NN` in messages).

## Open bugs

- **BUG-30 — `ProfileValidationRules` type signatures use `?int $userId` against UUID PKs.**
  Severity: HIGH. Surfaced by codebase audit 2026-04-27. The `app/Concerns/ProfileValidationRules.php` trait declared `profileRules(?int $userId = null)` and `emailRules(?int $userId = null)`, but the project uses UUID string primary keys on the `users` table (model has `HasUuids` + `$incrementing = false`). When `ProfileUpdateRequest::rules()` calls `$this->profileRules($this->user()->id)`, PHP threw `TypeError` before any validation ran. Locally this manifested as a Pest failure; in production every internal-user `/settings/profile` PATCH would 500. Two-character fix: `?int` → `?string` on both signatures. Test surface: `Tests\Feature\Settings\ProfileUpdateTest` × 2 (`profile information can be updated`, `email verification status is unchanged when the email address is unchanged`).

  Closed 2026-04-27 (commit `7ce6a5c`): two-character signature fix on the `ProfileValidationRules` trait plus a regression test (`profile update succeeds for a UUID-keyed user`) that asserts the User PK is a non-numeric string before exercising the PATCH — so a future re-regression to `?int` fails fast. Audit grouped 3 failing tests under this bug; investigation showed `RegistrationTest::test_new_users_can_register` is a separate issue (filed as BUG-31).

- **BUG-31 — Fortify `CreateNewUser` action does not set `users.role_id`; internal-user self-registration via `/register` is broken.** [NEEDS_PM_REVIEW]
  Severity: medium (route may not be a canonical path; no public-facing impact if the system disables it). Surfaced 2026-04-27 while diagnosing the audit's "3 failing tests" claim — the third failing test (`Tests\Feature\Auth\RegistrationTest::test_new_users_can_register`) is NOT caused by BUG-30; it fails because `App\Actions\Fortify\CreateNewUser::create()` writes only `name`, `email`, `password`, but the `users` table has a NOT NULL `role_id` column from the role-based RBAC schema. Resulting `QueryException`: `NOT NULL constraint failed: users.role_id` (sqlite) — would be a 23000 FK error in MySQL. Open scope question: should the public `/register` route exist at all in this internal closed system? Internal users (admin, procurement_officer, project_manager, evaluator) are normally created by an admin via `/admin/users`; vendor signup uses the separate `Vendor\RegisterController`. Two reasonable resolutions: (a) disable `Features::registration()` in `config/fortify.php`, drop the redundant `/register` route from the user-facing surface, and remove `RegistrationTest::test_new_users_can_register` (or convert to an "is this route 404?" assertion) — most secure, matches procurement-system threat model; (b) extend `CreateNewUser` to assign a default low-privilege role (e.g. `evaluator`) and keep self-registration — preserves the Starter Kit feature but introduces an unauthenticated path that creates internal accounts. **Awaiting Johnny's call before any code change.** While unresolved, this is the only failing test on `master`.

- **BUG-06 — `UpdateUserRequest` rejects `language_pref=ku`.**
  Severity: low (single missing enum value). The validation rule for `language_pref` in `app/Http/Requests/UpdateUserRequest.php` allows only `en` and `ar`. The application supports a third locale (`ku`, Kurdish) — it's already in the `lang/ku.json` file, the locale resolves correctly via Inertia, and the user's `language_pref` column accepts it at the DB level. The form request is the only chokepoint blocking admins from saving `ku` as a user's preferred language. Fix: add `'ku'` to the `Rule::in([...])` constraint on the `language_pref` field in `UpdateUserRequest::rules()`. Likely also exists on `StoreUserRequest` — audit both. Add a Pest test asserting `ku` is accepted. Side finding from BUGS.md drift triage 2026-04-27.

- **BUG-07 — `UserController::update` not transaction-wrapped — partial save risk on project-role sync failure.**
  Severity: medium (low frequency, real consequence). When an admin updates a user via `UserController::update`, the controller saves the user model and then syncs the user's project assignments (`projects()->sync(...)` or equivalent) as separate operations. If the sync fails (FK violation, network blip on the DB connection, validation throw mid-pivot-write), the user model is already saved with the new attributes but the project-role state is partially or fully wrong. Result: user looks updated in the admin list, but their access permissions don't match what was set. Fix: wrap the user save + project sync in `DB::transaction(...)` so a failure in either rolls both back. Mirror the pattern used in `AddendumController::store` post-BUG-26 (commit `9fb1f18`). Add a Pest test that forces the sync step to throw and asserts the user's pre-update attributes survive. Surfaced multiple times across sessions before being filed; not yet hit in production but the failure mode is concrete.

- **BUG-08 — Audit log writes don't capture all changed fields (system-wide).**
  Severity: medium (historical audit data is incomplete; new code uses the correct pattern post-BUG-26). The Laravel `getOriginal()` method called *after* `$model->save()` or `$model->update()` returns the **new** value, not the original — Eloquent overwrites the original attributes once the save lands. Several controllers in this codebase capture audit log values *after* the update call, which means their audit_log rows have identical `old_values` and `new_values` columns. The 7-year-immutable-audit-log promise from the project plan depends on this being correct system-wide.

  Action items:

  1. Grep all `AuditLog::create(...)` call sites: `grep -rn "AuditLog::create" app/`
  2. For each call site, verify the `old_values` array is built from a snapshot taken **before** the corresponding `update()` / `save()`, not after.
  3. Sites that capture post-save need refactoring to take a pre-save snapshot (pattern: `$old = $model->only([...])` before update, pass into `AuditLog::create` after).
  4. Add a system-wide convention test (or static analysis check) that catches the bad pattern automatically. One option: a Pest convention test that scans for `AuditLog::create` calls and asserts the surrounding code captured originals before the save.
  5. New code already uses the correct pattern (BUG-26 / `AddendumController::store` is the reference). Use that as the canonical example.

  Per-controller audit; estimated 2–3 days. The Tender model does NOT use the `owen-it/laravel-auditing` trait — this codebase is fully manual `AuditLog::create()`, no automatic capture, so every site needs verification.

- **BUG-12 — Wizard form architecture refactor (`useState` arrays + `useForm` scalars + `router.post`).**
  Severity: [JOHNNY: confirm — likely medium given it's "architecture refactor" not breakage]. [JOHNNY: please fill in the body. The original conversation context referenced "wizard form architecture" and a tension between three React patterns: `useState` for array fields, Inertia's `useForm` for scalar fields, and direct `router.post` calls. The refactor's goal and current pain point need your description.] Action items: [JOHNNY: list].

- **BUG-13 — Vendor portal ignores `language_pref` on initial render; hide `ku` from picker.**
  Severity: [JOHNNY: confirm — medium, likely]. Two related symptoms:

  1. When a vendor with `language_pref=ar` (or `ku`) logs in, the first render of the vendor portal lands in English regardless. The locale gets corrected on the next navigation, but the initial dashboard view is wrong. Compare against the MPC internal portal which doesn't have this issue.
  2. The locale picker in the vendor portal still exposes Kurdish (`ku`) even though `lang/ku.json` is full of `[en]` placeholder strings — vendors who pick it would see English labels with the layout direction NOT flipped. Either: (a) hide `ku` from the picker until full Kurdish translation lands (recommended interim), or (b) translate `ku` properly in the same i18n pass that closes BUG-25.

  Partial overlap with BUG-25 (vendor portal i18n coverage gap) noted during triage. [JOHNNY: confirm whether to merge BUG-13 into BUG-25 or keep separate — BUG-25 is broader (translation key coverage) while BUG-13 is more specific (initial-render bug + picker UX). My read: keep separate; they're two different fixes.]

- **BUG-17 — `POST /tenders` takes ~9 seconds when validation fails.**
  Severity: [JOHNNY: medium — perf bug, user-facing]. Symptom: when the tender create form posts and the server-side validation rejects (e.g., missing required field, invalid date format), the response takes ~9 seconds to return. A successful save is fast (<1s). The lag is on the failure path specifically, which is the more common path during real-world wizard use (admins iterate). [JOHNNY: please confirm whether this is still reproducible after recent wizard changes (BUG-22, BUG-15-related work). The symptom may have been incidentally fixed; if so, this should go straight to "investigate-then-close" rather than open the entry.] Possible causes to check: (a) Telescope or Pulse hooked into the request lifecycle in the validation-fail path even though they're disabled in prod, (b) translation file load on every request, (c) FormRequest's `authorize()` doing slow DB lookups before validation, (d) Inertia's error response payload generating slowly. Profile with Clockwork or `microtime(true)` markers at request start, FormRequest hit, validation reject, response render.

- **BUG-22 — Tender wizard silently drops invalid documents on publish.**
  Repro: as admin, run the tender create wizard. In the documents step, attach a non-PDF file (e.g. .jpg). Click Save & Publish. The tender publishes successfully, but the document does NOT upload — silently dropped, no validation error toast, no warning. POLICY-01 enforces PDF-only at the FormRequest layer, so the upload validation IS firing — but the wizard's submit handler treats partial-success as full-success and proceeds. Same class as BUG-15 (wizard publish silent failure on aggregate validation). Likely the wizard POSTs documents in a separate sub-request after tender creation, and that sub-request's validation rejection isn't caught by the wizard's promise chain or useForm error handler. High priority — the whole POLICY-01 enforcement story is undermined if the wizard silently bypasses it.

  Closed 2026-04-27 (commit `56cab6a`): two-layer fix. Frontend (`tender/Create.tsx`) — new `runDocumentPreflight()` runs BEFORE the POST, catches missing titles, non-PDF mime, and >5 MB size on each row; toast + auto-jump to Documents step. `onError` also auto-jumps when any server error key starts with `documents.*` so the inline FieldError becomes visible from step 5 (Review). Review step renders a destructive Alert + disables Save & Publish when no document rows exist (belt-and-suspenders with the new backend gate). Backend (`TenderService::assertPublishPrerequisites`) — now requires at least one current document, throws `TenderPublishException` with `messages.publish_reason_no_documents` (mirrors the existing BOQ + criteria gates from BUG-16). New service-level Pest test plus updates to `TenderFactory::withBoqAndCriteria()`, `tenderPayload`, and `validTenderPayload` so existing publish tests survive the new gate. Surfaced **TECH-DEBT-06** (file uploads aren't transaction-aware: rollback leaves S3 orphans) — worked around in T-C-02 / T-C-34 by overriding `documents => []`.

- **BUG-24 — audit_logs schema inadequate for MySQL strict mode (HIGH severity).**
  6 tests pass under sqlite but fail under MySQL strict mode with `SQLSTATE[22001]: Data too long for column 'auditable_id'`. The audit log is the project's "immutable 7-year record" per the project plan; silent column truncation in production would be a compliance hole. Sqlite tolerates oversize values, MySQL truncates — meaning local tests have been hiding this since the audit_logs table was introduced.
  Action items:
  1. Run on prod: `SELECT COUNT(*) FROM audit_logs WHERE LENGTH(auditable_id) >= 250` — see how often we've been at the truncation boundary.
  2. Audit which fields could exceed 255 chars (UUIDs are 36 — well under — but composite keys, JSON-encoded payloads, or URL-style paths could blow it).
  3. Widen `auditable_id` and any other narrow columns to TEXT or VARCHAR(1024) via forward-compat migration.
  4. While we're in the table: audit `auditable_type`, `action`, `ip_address`, `user_agent` for similar issues.
  5. Add a test that explicitly creates a long auditable_id under MySQL to prove the fix and prevent regression.

- **BUG-23 — Addenda tab on admin tender detail page has no "Issue Addendum" button or upload zone.**
  Repro: as admin, navigate to a Published, non-cancelled, non-past-deadline tender's detail page (e.g. MBP-T006 "Two Envelope Test Bug 18"). Click the Addenda tab. Result: empty state "No addenda have been issued for this tender." with no button or affordance to issue one. There is no way to upload an addendum from the UI even though StoreAddendumRequest, the route, and the file-upload pattern all exist on the backend (verified via POLICY-01 audit which swapped the addendum FormRequest to PdfFile rule). Likely a missing UI control on resources/js/pages/tender/Show.tsx in the Addenda tab section — either the "Issue Addendum" button was never rendered, was removed, or is gated by a permission check that's wrong. Backend works, frontend doesn't expose it. Medium-high priority — addenda are a procurement requirement (clarifications + corrections to published tenders pre-deadline), and this gap forces admins to bypass the system entirely.

  Closed 2026-04-27 (commit `8eef587`): split addendum-form gate from canEdit. New `canIssueAddendum` prop computed in `TenderController::show` checks `tenders.issue_addenda` permission AND `status === Published` (mirrors `StoreAddendumRequest::authorize`). canEdit (Draft-only via `update` policy) remains correct for tender-content edits; the addendum form now correctly appears on Published tenders. 8 other canEdit references in `tender/Show.tsx` left untouched (they correctly cover tender-content edits, not amendments).

- **BUG-25 — Vendor portal UI strings not translated when locale=ar (translation key coverage gap).**
  Reproduced on Docker stack 2026-04-26: Fatima (language_pref=ar) logs into /vendor/login. RTL layout flips correctly (sidebar moves to right, Arabic locale toggle visible at bottom-right, dir=rtl applied) — confirming locale resolution and layout direction both work. However, all UI strings stay in English: "Welcome, Erbil MEP Solutions", "Open Tenders", sidebar items (Dashboard, My Bids, Notifications, Profile, Documents, Categories, Category Requests), section headers (Recent Bids, Prequalification Status), and empty states (No bids have been submitted). This is NOT a layout/RTL bug — it's a translation key coverage gap on the vendor portal. Sibling effort to the partial i18n work shipped in commit 37482d6 (toast notifications only). Severity: medium-high — vendor portal is the supplier-facing surface, and Arabic-first vendors will see a half-translated UI.

  Action items:
  1. Audit `resources/js/pages/vendor/**` and `resources/js/layouts/vendor/**` for hardcoded English strings (look for JSX text content not wrapped in `t('...')`).
  2. Cross-check `lang/en.json` vs `lang/ar.json` for missing keys in the `vendor.*` and `dashboard.*` namespaces.
  3. Apply Strategy A pattern (DB stores slug, React renders via `t('scope.${key}.label')`) consistently. The screenshot shows tender titles are also English-only, but those are seeded fixture descriptions (not real tender content) — that's a seeder choice, not a translation bug.
  4. Add a Pest test that switches a user's `language_pref` to `'ar'` and asserts at least one Arabic-specific string appears on `/vendor/dashboard`, to catch regressions.

  Note: this work is part of a broader i18n sweep that was previously scoped under a prompt named `PROMPT_I18N_COMPLETE_TRANSLATION.md` (referenced in `MPC_eTender_Release_Notes_v2_0.docx` but not yet checked into the repo). When tackling BUG-25, lift that prompt's scope into a project doc (e.g. `docs/I18N_SWEEP_PLAN.md`) so it survives outside Word docs.

- **BUG-26 — Addendum deadline extension does not cascade to opening date.**
  High severity. On tender `MBP-T008` (production verification, April 27, 2026), issuing an addendum with "Extend Deadline" checked updates `submission_deadline` but leaves `opening_date` untouched, producing an invalid state where `submission_deadline > opening_date` — bids cannot be opened until after they are submitted. Blocks Step 7 of the test plan (vendor bid submission against `MBP-T008` will hit this state). Action items:

  1. In `AddendumController::store` (and the corresponding service/action), when `extend_deadline` is checked recalculate `opening_date` so it remains strictly after the new submission deadline.
  2. Add a configurable `min_hours_between_deadline_and_opening` setting (default 24h) and enforce it in `AddendumRequest` and `UpdateTenderRequest`.
  3. In the "Issue Addendum" form, when "Extend Deadline" is checked expose a second field for the new opening date, pre-filled with `new_deadline + buffer`.
  4. Backfill check: Tinker one-off to identify any existing tenders where `submission_deadline >= opening_date` and flag them for procurement officer review.
  5. Pest tests: (a) addendum extending deadline keeps opening date strictly after the new deadline; (b) validation error when `opening_date <= submission_deadline`.

  Closed 2026-04-27 (commit `9fb1f18`, prerequisite `8eef587` for BUG-23 form-gate split): cascade implemented in `AddendumController::store` (DB::transaction wrap, AuditLog::create capturing both `submission_deadline` and `opening_date` old/new values, pre-save snapshot per BUG-08 gotcha pattern). New `tender.min_hours_between_deadline_and_opening` setting (default 24h) enforced via custom `MinHoursAfter` rule in `StoreAddendumRequest`. React form (`tender/Show.tsx`) renders both date fields side-by-side with reactive pre-fill (`new_deadline + bufferHours` from Inertia shared props). 8 Pest tests passing (T-A-03 happy path updated, T-A-04..T-A-09 added covering validation, audit, transaction rollback, no-cascade case). Browser-verified local en + ar (RTL grid flips naturally via Tailwind's logical `grid-cols-2`). Production backfill (`submission_deadline >= opening_date`) returned 1 tender in bad state: **MBP-T008** (id `019dcba6-16ed-7258-ad24-d6d3207cca8f`, submission_deadline `2026-04-30 00:18:00 UTC`, opening_date `2026-04-29 10:00:00 UTC`, delta -14h18m). Needs a procurement-officer-issued corrective addendum using the now-deployed cascade form — not auto-fixed via tinker, since the procurement officer is the canonical authority for tender date changes. Once the corrective addendum lands, MBP-T008 is unblocked for Step 7 of the test plan (vendor bid submission).

- **BUG-27 — Admin dashboard is empty; scope decision pending.**
  Medium severity. `/admin/dashboard` currently has no content. Need a scope decision before implementation. Proposed split to avoid overlap with `/dashboard` (personal) and `/dashboard/portfolio` (cross-project KPIs):

  - `/admin/dashboard` → system health & governance: total users by role, active vs suspended vendors, pending vendor prequalifications queue, recent audit log entries (last 50), failed jobs from Horizon, queue depth, storage usage, recent role/permission changes.

  Pragmatic Phase 1 alternative: embed the portfolio view in the admin dashboard for now and split when system-health metrics are ready. Sponsor / PM to confirm direction before development begins. Estimated effort 2–3 days once scope is locked.

- **BUG-28 — Verify 2FA implementation end-to-end.**
  Severity: medium (briefly raised to high on 2026-04-27 between investigation commit `218f6ac` and interim mitigation commit `6d3521e` — the interim mitigation removes the false-security signal so the gap reverts to a missing-feature classification rather than a misleading-control one). Admin → Settings shows "2FA mandatory for internal users" toggle as enabled, but no internal user has been forced through 2FA enrollment during testing — the toggle may be cosmetic. Investigation checklist:

  1. Confirm `users` table has `two_factor_secret`, `two_factor_recovery_codes`, `two_factor_confirmed_at` columns.
  2. Confirm Laravel Fortify is installed and `Features::twoFactorAuthentication()` is enabled in `config/fortify.php`.
  3. Confirm an `EnsureTwoFactorAuthenticated` middleware (or equivalent) is registered on internal routes.
  4. Locate the user-facing enrollment flow (QR code page, recovery codes display, confirmation step).
  5. Verify the Settings toggle actually drives enforcement rather than being decorative.
  6. Confirm scope is correctly limited to internal users only — vendor portal must be unaffected.
  7. Add Pest tests covering enrollment and login challenge flows.

  Acceptance criteria: when toggle is ON, internal users without 2FA configured are redirected to enrollment on next login and cannot access the dashboard until enrolled. When OFF, 2FA is optional but available. Vendor portal unaffected by the toggle in either case.

  Investigation 2026-04-27: verdict **COSMETIC**. Fortify (`^1.34`) + `pragmarx/google2fa` (`^9.0`) installed (task 1); `config/fortify.php` declares `Features::twoFactorAuthentication([confirm, confirmPassword])` (task 2); User model uses `TwoFactorAuthenticatable` trait with all 3 columns in `$hidden`/`$casts` (task 3); `users` table has `two_factor_secret`, `two_factor_recovery_codes`, `two_factor_confirmed_at` (task 4); Fortify auto-registers enrollment + challenge routes (task 5); React UI exists for `/auth/two-factor-challenge`, setup modal, and recovery codes (task 9); vendor scoping is correct — vendors use a separate `'vendor'` guard with no 2FA columns (task 10); `TwoFactorChallengeTest` covers the voluntary flow (task 11). **But: no enforcement middleware exists** (task 6 — zero matches for `EnsureTwoFactor` / `RequireTwoFactor` / `two_factor_confirmed_at` checks in `app/Http/Middleware`, `bootstrap/app.php`, or `routes/`), **and the setting key `security.2fa_mandatory_internal` has zero readers** (task 8 — only writer is `SettingController`; no code path consults it). Currently 2FA is entirely volunteer-only: a user with `two_factor_confirmed_at` set gets challenged at login; a user without it proceeds to dashboard. The toggle changes a row in `system_settings` and nothing else. Locally 0/1 users enrolled (task 12). **Recommendations:** (a) re-classify BUG-28 severity MEDIUM → HIGH — false-security signal is worse than no toggle; (b) BUG-28 should jump ahead of BUG-26 in priority; (c) immediate interim mitigation: hide the toggle behind a "Coming soon" badge or remove it from the Settings page until enforcement ships (~0.5 day); (d) full implementation (build `EnsureTwoFactorAuthenticated` middleware, wire to `auth('web')` route group, gate on the setting, expand Pest coverage) ~5–7 days. Side finding: `users.is_2fa_enabled` column appears redundant with Fortify's `two_factor_confirmed_at` — worth a cleanup pass when 2FA work is tackled.

  Interim mitigation 2026-04-27 (commit `6d3521e`): toggle disabled in UI, "Coming soon" badge added (en: "Coming soon", ar: "قريباً"), persisted setting value untouched. Implemented as Pattern A — inline early-return in `resources/js/pages/admin/Settings/Index.tsx` `renderSettingInput` keyed on the literal `'security.2fa_mandatory_internal'` (the admin Settings page is data-driven, so no per-toggle JSX existed to disable; the early-return is the surgical knob that singles this one key out without affecting other boolean settings). Full enforcement build (~5–7 days) deferred to a later cycle, blocked behind BUG-26 and tracked alongside TECH-DEBT-09. Verification screenshots: `docs/bug-28-coming-soon-en.png`, `docs/bug-28-coming-soon-ar.png`.

## Open bugs (cosmetic / non-blocking)

- **BUG-21 — Parent Category dropdown shows stray stepper arrows on /admin/categories.**
  Below the "Parent Category" Select on the categories admin page, a vertical up/down arrow control renders unexpectedly. Likely either a numeric stepper bleeding from a misapplied input type, a duplicated chevron, or a native `<select>` size attribute. Cosmetic only — the dropdown still functions. Reproduce: navigate to /admin/categories as admin, scroll to the "Add Category" form. Screenshot in BUG-21 attachment (Johnny's WhatsApp 2026-04-26).

- **BUG-29 — Vendor document type labels showing raw i18n keys.**
  Severity: low. Same class as BUG-03. On `/vendor/documents`, the Document Type dropdown renders raw keys (`vendor.doc_type_trade_license`, `vendor.doc_type_tax_certificate`, `vendor.doc_type_insurance`, `vendor.doc_type_financial_statement`, `vendor.doc_type_bank_reference`, `vendor.doc_type_experience_certificate`, `vendor.doc_type_iso_certificate`, `vendor.doc_type_other`) instead of resolved labels. Apply Strategy A: confirm DB stores slugs (`trade_license`, `tax_certificate`, …), render via ``t(`vendor.doc_type.${slug}.label`)`` in React, add 8 keys to each of `lang/en.json` and `lang/ar.json`, dev-mode `console.warn` on missing keys per existing convention. Worth batching with BUG-03 and any other DB-seeded enum-like values (category names, role names, status labels) into one Strategy A cleanup commit.

- **BUG-05 — MultiSelect renders nested `<button>` HTML (a11y / keyboard nav).**
  Severity: low (cosmetic — not visually broken, but reads as a violation when assistive tech walks the DOM). The shadcn `<MultiSelect>` primitive currently renders a `<button>` element inside another `<button>` (likely the chip-with-X-to-remove pattern inside the trigger button). Browsers tolerate it; screen readers and keyboard navigation get confused. Fix: refactor the inner element from `<button>` to `<span role="button" tabIndex={0}>` with explicit `onKeyDown` for Enter/Space, OR restructure so the chip's remove control sits outside the trigger button entirely. Verify with axe-core or browser devtools accessibility tree. Surfaces wherever MultiSelect is used (categories, vendor categories, role permissions). Carried as a known issue across multiple sessions before being filed in this housekeeping pass.

## Tech debt

- **TECH-DEBT-01 — Unify file upload patterns across vendor pages.**
  Three vendor pages currently hand-roll their own file upload UI: `vendor/Documents/Index.tsx`, `vendor/CategoryRequests/Create.tsx`, and (post-BUG-18 Sub-B) `components/FileUpload.tsx`. The new `FileUpload` component is opinionated for bid documents (PDF only, 5 MB, fixed `BidDocType` list). When a fourth upload pattern shows up (e.g. tender documents v2, vendor profile attachments), revisit and extract a more general `<FileUploadField>` that can be parameterised on mime, size, and doc-type list — generalising prematurely now would compromise the bid-doc ergonomics.

- **TECH-DEBT-02 — FileUploadService uses extension-based validation, not mime-sniffing.**
  `getClientOriginalExtension()` on line ~24 trusts the filename. Laravel's `mimes:pdf` request rule does real content sniffing, so this is a defense-in-depth weakness, not an exploitable hole. When making the service parameter-driven for size/mime (per POLICY-01 service signature TODO), also switch to `$file->getMimeType()`.

- **TECH-DEBT-03 — UserFactory random language_pref causes flaky test (BUG-14 assertion).**
  `UserFactory` line 28 uses `fake()->randomElement(['en', 'ar'])` for language_pref. The SetLocale middleware respects it, so any test that asserts English copy fails ~50% of the time when the factory rolls Arabic. Fix: default to 'en', add an `->arabic()` factory state for tests that explicitly want Arabic. Affects CreateTenderPublishTest > "validation error message uses user-readable label (BUG-14)" today; likely affects other locale-sensitive tests not yet caught.

- **TECH-DEBT-04 — phpunit.xml `<env>` blocks lack `force="true"`.**
  Without `force="true"`, phpunit's env overrides only apply when the named env var is unset — they DON'T override values already in `.env`. Inside the Docker stack `.env` sets `DB_CONNECTION=mysql`, so `./vendor/bin/pest` runs against MySQL instead of the intended sqlite `:memory:`. The Makefile's `make test` target works around it via explicit `-e DB_CONNECTION=sqlite`. Fix: add `force="true"` to all `<env>` blocks in phpunit.xml. Note: this only fixes the *config drift*; the underlying MySQL strict-mode failures it exposes are filed separately as BUG-24.

- **TECH-DEBT-05 — App image is 3.61 GB (Sail Ubuntu base).**
  Could shrink to ~400 MB with a custom `docker/app/Dockerfile` based on `php:8.4-fpm-alpine` + multi-stage Composer/Node install. Not blocking daily dev — only matters when rebuilding from scratch (e.g. after `composer require`). Defer until rebuild times become annoying. The Sail-based approach was chosen for speed-to-prod-parity, not image minimalism — see docs/DEVELOPMENT.md.

- **TECH-DEBT-06 — File uploads aren't transaction-aware: rolled-back creates leave S3 orphans.**
  `TenderService::create` wraps DB writes in `DB::transaction`, but `FileUploadService::upload` writes to S3 synchronously and returns the path before the transaction commits. If a later step in the same transaction throws (e.g. a publish-prereq exception), the DB rolls back cleanly but the uploaded files persist in S3 as orphans with no DB row pointing at them. Surfaced by BUG-22 fix: after adding documents to the canonical `tenderPayload` fixture, T-C-02 and T-C-34 (which assert "no S3 files after rollback") started failing because docs upload before the publish-prereq throw. Worked around by overriding `documents => []` in those two tests. Real fix: either (a) defer S3 writes to after-commit hooks, or (b) collect uploaded paths during the transaction and `Storage::delete()` them in the catch block. (b) is simpler; (a) is more robust. Severity: low (orphans waste storage but don't corrupt data) — bump if storage costs become noticeable.

- **TECH-DEBT-07 — Vite production build fails inside the app container due to native binary mismatch.**
  `docker compose exec app npm run build` fails with "native rolldown binary mismatch" because `node_modules` was populated by `npm install` running on the Windows host — meaning the binaries inside `node_modules/@rollup/...` are Windows-native and the Linux container can't execute them. Workaround for this PR: run `npm run build` on the host. Real fix: either (a) add `node_modules` to the volumes-not-mounted list in compose.yaml so the container has its own Linux-native install, or (b) add a `make npm-install` target that always runs `npm install` inside the container, documented as a first-time-setup step in DEVELOPMENT.md. Severity: medium — currently breaks the dev-stack-parity story (we wanted local builds to match prod builds; right now they don't). Surfaced during BUG-22 verification.

- **TECH-DEBT-09 — Redundant `users.is_2fa_enabled` column.**
  Surfaced during BUG-28 investigation (commit `218f6ac`). The `users` table has both `is_2fa_enabled` (custom column) and Fortify's `two_factor_confirmed_at` (set when a user confirms enrollment with a valid TOTP code). They encode overlapping state. When the full BUG-28 enforcement build is scheduled, drop `is_2fa_enabled` and use `two_factor_confirmed_at IS NOT NULL` as the canonical "is this user 2FA-enrolled" predicate everywhere. Migration + grep sweep for any code reading `is_2fa_enabled`. Severity: low. Blocked on BUG-28 full build (don't drop the column until enforcement is wired and we've confirmed nothing else depends on it).

- **TECH-DEBT-10 — Per-envelope opening dates + two-stage seal/open for two-envelope tenders.**
  Two-envelope tenders today use the same `opening_date` for both technical and financial envelopes. Procurement procedure says technical envelope opens first, evaluation runs, then financial envelope opens for technically-passing bids only — so they need two separate dates, persisted independently, and the bid-opening flow has to gate financial-envelope decryption on technical-envelope evaluation completion. [JOHNNY: confirm scope — is this Phase 1 procurement-process essential, or Phase 2 enhancement? My read: it's procurement-essential for any genuine two-envelope tender, which means it's technically blocking proper M-04 evaluation. But "technically blocking" may not match real procurement behavior at MPC — if you've been running two-envelope tenders with the same opening date in production successfully, the priority is lower than I'd guess. Estimated 1 sprint once scope is locked.] Surfaced in BUG-18 Sub-A's commit message as future scope.

- **TECH-DEBT-08 — Tender Shortlist tab and workflow.**
  New feature in M-04 Evaluation Engine domain. Add a "Shortlist" tab to the tender detail page (alongside Overview, BOQ, Documents, Addenda, Clarifications, Evaluation) for managing post-evaluation shortlists before final award. Scope definition required before implementation. Open design questions:

  1. Auto-populated from evaluation results (top N by score) or manually curated by the committee?
  2. Does shortlisting trigger a separate notification flow to shortlisted vendors (WhatsApp + Email per existing matrix)?
  3. Is there a BAFO (Best And Final Offer) round between shortlisting and award, with its own bid revision workflow?
  4. For two-envelope tenders: does shortlisting happen after technical pass and before financial envelope opening?
  5. Permissions: confirm view/edit scope (likely evaluation committee + procurement head).
  6. Does shortlist status feed into vendor performance scoring for future tenders (Phase 2 supplier performance module)?

  Likely data model: new `tender_shortlists` table linking `tender_id` → `bid_id` with status enum (`shortlisted`, `bafo_invited`, `awarded`, `rejected`); audit log entries on every shortlist change; `ShortlistPolicy` for view/create/update permissions. Dependencies: M-04 complete and tested; M-05 notification engine extension for shortlist events. Estimated effort: ~1 sprint (10 working days) once scope is locked.
